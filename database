create schema CASINO;

CREATE TABLE CASINO.USER (
IDUSER int NOT NULL,
NOMBRE VARCHAR(50) NOT NULL,
APPATERNO VARCHAR(50) NOT NULL,
APMATERNO VARCHAR(50) ,
CONTRASEÑA VARCHAR(50) NOT NULL,
DINERO DECIMAL(15,3) default 0,
SEXO varchar(1)
,PRIMARY KEY (IDUSER)
);
